---
interface Props {
  id: string;
  num: number;       // 1..4, shown as "02 / 04"
  dark?: boolean;
  minH: string;      // starting height, content can push past it
}
const { id, num, dark = false, minH } = Astro.props;
const pad = (n: number) => String(n).padStart(2, '0');
---
<section id={id} class:list={['band', { dark }]} style={`--min-h:${minH}`} aria-labelledby={`${id}-title`}>
  <div class="wrap">
    <p class="chapter"><span>{pad(num)} / 04</span><span class="rule"></span></p>
    <slot />
  </div>
</section>

<style>
  .band { min-height: var(--min-h); padding: calc(var(--band-pad) * 0.55) 0 var(--band-pad); display: flex; flex-direction: column; justify-content: center; }
  .band > .wrap { width: 100%; }
  .dark { background: var(--navy); color: var(--paper-ink); }
  .chapter { display: flex; align-items: center; gap: 1.1rem; margin-bottom: 0.8rem; font-size: 0.95rem; letter-spacing: 0.06em; }
  .rule { width: 84px; border-top: 1px solid currentColor; }
</style>
